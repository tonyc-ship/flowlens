# Legacy FlowLens Desktop Spike

This Tauri app is archived for reference only. It used to live at the top-level `desktop_app/` path.

Active desktop development now happens in `app/`.

Do not add new product work here unless intentionally inspecting or migrating legacy behavior.
